#include<iostream>
#include<cmath>

using namespace std;

int func(int x);

int main(void)
{
  long long N, a = 1, b = 0;
  cin >> N;

  while(N != (a + b) * (a * a + b * b)) {

  }

int func(long long a, long long b)
{
  return (a + b) * (a * a + b * b);
}